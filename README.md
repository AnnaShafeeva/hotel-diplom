# Hotel Diplom - Агрегатор бронирования гостиниц

Дипломный проект Fullstack-разработчика на JavaScript - сайт-агрегатор для просмотра и бронирования гостиниц.

## 📖 Описание проекта

Fullstack приложение для поиска и бронирования гостиничных номеров с интегрированной системой чата поддержки. Проект включает фронтенд на React и бэкенд на NestJS с полным циклом бронирования и обслуживания клиентов.

### 🎯 Основная функциональность

**Публичная часть (без авторизации):**
- 🔍 Поиск номеров по датам заезда/выезда
- 🏨 Просмотр информации об отелях и номерах
- 📸 Галерея изображений номеров
- 📄 Пагинация и фильтрация результатов

**Для зарегистрированных клиентов:**
- ✅ Бронирование номеров на выбранные даты
- 📋 Просмотр истории своих бронирований
- ❌ Отмена бронирований
- 💬 Чат с техподдержкой (создание обращений, обмен сообщениями)
- 🔔 Real-time уведомления о новых сообщениях

**Для менеджеров:**
- 👥 Просмотр всех пользователей системы
- 📊 Просмотр бронирований любого пользователя
- 🗂️ Управление обращениями в поддержку
- 💬 Ответы в чатах поддержки
- 🔒 Закрытие обращений

**Для администраторов:**
- 🏨 Полное управление отелями (добавление, редактирование)
- 🛏️ Управление номерами (с загрузкой изображений)
- 👥 Управление пользователями (создание, просмотр)
- ⚙️ Настройка системы

### 🏗️ Техническая архитектура

**Frontend:**
- React 19 с TypeScript
- Redux Toolkit для управления состоянием
- Material-UI (MUI) для компонентов интерфейса
- React Router для навигации
- Axios для HTTP-запросов
- Socket.io-client для WebSocket соединений
- Vite как сборщик

**Backend:**
- NestJS 11 с TypeScript
- MongoDB с Mongoose ODM
- JWT аутентификация с Passport
- Socket.io для real-time коммуникации
- Class-validator для валидации DTO
- Multer для загрузки файлов
- Bcrypt для хеширования паролей

**База данных:**
- **MongoDB 8.2.1** с коллекциями: `users`, `hotels`, `hotelrooms`, `reservations`, `supportrequests`, `messages`
- **База данных**: `hotel-app`
- **Порт**: 27017

## 📁 Структура проекта
hotel-diplom/
├── backend/ # NestJS бэкенд приложение
│ ├── src/
│ │ ├── auth/ # Модуль аутентификации и авторизации
│ │ ├── common/ # Общие утилиты и типы
│ │ ├── hotels/ # Модуль управления отелями
│ │ ├── reservations/ # Модуль бронирований
│ │ ├── support/ # Модуль чата поддержки
│ │ ├── users/ # Модуль пользователей
│ │ ├── app.controller.ts
│ │ ├── app.module.ts
│ │ ├── app.service.ts
│ │ └── main.ts
│ ├── test/ # Jest тесты
│ ├── uploads/ # Загруженные изображения отелей
│ ├── Dockerfile # Production Dockerfile
│ ├── Dockerfile.dev # Development Dockerfile
│ ├── package.json # Зависимости бэкенда
│ └── .env # Переменные окружения
├── frontend/ # React фронтенд приложение
│ ├── src/
│ │ ├── components/ # React компоненты
│ │ ├── pages/ # Страницы приложения
│ │ ├── services/ # API сервисы
│ │ ├── store/ # Redux store
│ │ ├── types/ # TypeScript типы
│ │ ├── App.tsx # Корневой компонент
│ │ └── main.tsx # Точка входа
│ ├── vite.config.js # Конфигурация Vite
│ └── package.json # Зависимости фронтенда
├── mongo-data/ # Docker volume для данных MongoDB
├── docker-compose.yml # Production Docker Compose
├── docker-compose.dev.yml # Development Docker Compose
└── README.md # Эта документация

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+ 
- MongoDB 5.0+
- Docker и Docker Compose (опционально)

### Запуск с Docker

```bash
# Клонирование репозитория
git clone <repository-url>
cd hotel-diplom

# Запуск в development режиме
docker-compose -f docker-compose.dev.yml up --build
```

### Ручная установка

```bash
# Запуск MongoDB
docker run -d -p 27017:27017 --name mongo -v mongo-data:/data/db mongo

# Бэкенд (терминал 1)
cd backend
npm install
npm run start:dev

# Фронтенд (терминал 2)
cd frontend
npm install
npm run dev
```

### 🌐 Доступ к приложению
После успешного запуска приложение доступно по адресам:
- Фронтенд приложение: http://localhost:3001
- Бэкенд API: http://localhost:3000
- MongoDB: localhost:27017 (база данных hotel-app)

## 👥 Система ролей

- **client** - Клиенты
- **manager** - Менеджеры
- **admin** - Администраторы

## 📊 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы  
- `POST /api/client/register` - Регистрация нового клиента

### Отели и номера (публичные)
- `GET /api/common/hotel-rooms` - Поиск номеров
- `GET /api/common/hotel-rooms/:id` - Информация о конкретном номере

### Бронирования
**Для клиентов:**
- `POST /api/client/reservations` - Бронирование номера клиентом
- `GET /api/client/reservations` - Список броней текущего пользователя
- `DELETE /api/client/reservations/:id` - Отмена бронирования клиентом

**Для менеджеров:**
- `GET /api/manager/reservations/:userId` - Список броней конкретного пользователя
- `DELETE /api/manager/reservations/:id` - Отмена бронирования менеджером

### Чат поддержки
**Для клиентов:**
- `POST /api/client/support-requests` - Создание обращения в поддержку
- `GET /api/client/support-requests` - Получение списка обращений для клиента

**Для менеджеров:**
- `GET /api/manager/support-requests` - Получение списка обращений для менеджера
- `DELETE /api/manager/support-requests/:id` - Закрытие обращения

**Общие (клиенты и менеджеры):**
- `GET /api/common/support-requests/:id/messages` - Получение истории сообщений из обращения
- `POST /api/common/support-requests/:id/messages` - Отправка сообщения
- `POST /api/common/support-requests/:id/messages/read` - Отправка события, что сообщения прочитаны

### Управление пользователями
**Для администраторов:**
- `POST /api/admin/users/` - Создание пользователя
- `GET /api/admin/users/` - Получение списка пользователей

**Для менеджеров:**
- `GET /api/manager/users/` - Получение списка пользователей

### Управление отелями (только администраторы)
- `POST /api/admin/hotels/` - Добавление гостиницы
- `GET /api/admin/hotels/` - Получение списка гостиниц
- `PUT /api/admin/hotels/:id` - Изменение описания гостиницы
- `POST /api/admin/hotel-rooms/` - Добавление номера
- `PUT /api/admin/hotel-rooms/:id` - Изменение описания номера

  ## 🔒 Безопасность

### Валидация входных данных
- Все входные данные валидируются на бэкенде с использованием `class-validator`
- Проверка формата email, длины пароля, обязательных полей
- Валидация загружаемых файлов (изображений отелей)

### JWT аутентификация
- Аутентификация через JSON Web Tokens (JWT)
- Токены хранятся в localStorage фронтенда
- Автоматическое обновление токенов через interceptors
- Защищенные маршруты с ролевыми guards

### Защита от XSS-атак
- Экранирование пользовательского ввода перед отображением
- Безопасная обработка HTML-контента
- CSP-политики на фронтенде

### Безопасная передача паролей
- Пароли хешируются с использованием `bcrypt` перед сохранением в БД
- Передача паролей только через HTTPS (в production)
- Отсутствие логирования чувствительных данных

### Ролевая модель доступа
- Разделение прав доступа по ролям: client, manager, admin
- Проверка прав доступа к ресурсам на бэкенде
- Защита API endpoints от несанкционированного доступа

### Защита базы данных
- Валидация данных на уровне Mongoose схем
- Инъекция-безопасные запросы к MongoDB
